// What is typeScript
// TypeScript is openSource  object oriented pragraming language by microsoft angular me use hoti hai .ts extantion ke sath
//  typeScript have a Compiler (its a benefit) so we can get error in compile time but javaScript have a intpreter which gives error in run time isme complier ni transfiler hota hai - ( if broweser dont know code of ES6 than transfiler covert that code into ES5  )
// TypeScript extends JavaScript and improves the developer experience. It enables developers to add type safety to their projects. Moreover, TypeScript provides various other features, like interfaces, type aliases, abstract classes, function overloading, tuple, generics, etc.
// TypeScript allows specifying the types of data being passed around within the code, and has the ability to report errors when the types don't match. For example, TypeScript will report an error when passing a string into a function that expects a number. JavaScript will not. TypeScript uses compile time type checking.

/// CSS  target attribute of anchar tag diff. :-
// target=_blank - links Open always in new Tabs BUT target="blank" means if you click one link and during this opening time you will click any link of page then which is not open new tab usi tab me open ker dega jo open ho raha tha...
// The difference: target="_blank" is a special keyword that will open links in a new tab every time. target="blank" will open the first-clicked link in a new tab, but any future links that share target="blank" will open in that same newly-opened tab

//// Base64  -  base64 is encodeing and decodeing technique use to convert binary data into ASCII format
//  for uploading img to mongoDB we need convert that img in base64 format // img store in the data base as a string

// // this object will not work with arrow function bcz arrow function is bound to class.

// Custom hooks :-
// useInput.js
// Example :-
// import { useState } from 'react';
// const useInput = (initialValue) => {
//  const [value, setValue] = useState(initialValue);
//  const handleChange = (e) => {
//  setValue(e.target.value);
//  };
//  return {
//  value,
//  onChange: handleChange,
//  };
// };
// export default useInput


// rendering Props :- “Render Props is a pattern in React where a function is passed as a prop to share reusable logic between components, allowing dynamic rendering based on that logic.”


//. aggrigation pipeline
// NEST
// what are the challenges face building firstsources project

